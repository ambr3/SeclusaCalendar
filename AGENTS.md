# Project Rules

- Work only within this project folder (`/home/ambr3/opencode/privacy-calendar`).
- Never push to git without asking the user first.
- Never bump the version in any `index.html`, `offline.html`, `manifest.json`, or `sw.js` without asking the user first — propose the exact version and get approval before changing it.